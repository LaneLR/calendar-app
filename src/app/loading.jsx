const loading = () => {
  return (
    <div
      style={{
        fontSize: "3.5rem",
        padding: "0 0 50px 0",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flexGrow: "1",
        minHeight: "80vh",
      }}
    >
      Loading...
    </div>
  );
};

export default loading;
